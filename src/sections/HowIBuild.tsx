/** How I Build — the amber pipeline draw lands in Milestone 4. Shell only. */
import { Section } from '../components/Section';

export function HowIBuild() {
  return <Section id="how" index="03 · Process" title="How I Build" />;
}
