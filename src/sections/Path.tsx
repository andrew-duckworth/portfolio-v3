/** Path — scrubbed vertical timeline lands in Milestone 4. Shell only. */
import { Section } from '../components/Section';

export function Path() {
  return <Section id="path" index="05 · Path" title="Path" />;
}
