/** Stack — marquee + chip settle lands in Milestone 4. Shell only. */
import { Section } from '../components/Section';

export function Stack() {
  return <Section id="stack" index="04 · Stack" title="Stack" />;
}
