/** Contact — finale with the Forge Core callback lands in Milestone 4. Shell only. */
import { Section } from '../components/Section';

export function Contact() {
  return <Section id="contact" index="06 · Contact" title="Let's build something" />;
}
