/** Hero — the Forge Core lands here in Milestone 3. Shell only for now. */
export function Hero() {
  return (
    <section id="hero" className="section" aria-label="Introduction">
      <p className="section__index">00 · Hero</p>
      <h1 className="section__title">I build AI systems that ship.</h1>
    </section>
  );
}
