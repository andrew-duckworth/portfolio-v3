/** Featured Work — horizontal pinned gallery lands in Milestone 4. Shell only. */
import { Section } from '../components/Section';

export function FeaturedWork() {
  return <Section id="work" index="02 · Work" title="Featured Work" />;
}
